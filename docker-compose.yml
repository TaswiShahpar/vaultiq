version: '3.8'

services:
  vaultiq:
    build: .
    ports:
      - "8000:8000"
      - "8501:8501"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - ./vectorstore:/app/vectorstore
      - ./uploads:/app/uploads
    restart: unless-stopped